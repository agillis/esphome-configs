substitutions:
  name: athom-rgb-light-d93662

packages:
  athom.rgb-light: !include modules/athom-rgb-light.yaml

esphome:
  name: ${name}
  name_add_mac_suffix: false

api:

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  min_auth_mode: WPA2
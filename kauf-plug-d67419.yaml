substitutions:
  name: kauf-plug-d67419

packages:
  kauf.plug: !include modules/kauf-plf12.yaml

esphome:
  name: ${name}
  name_add_mac_suffix: false

api:

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  min_auth_mode: WPA2
